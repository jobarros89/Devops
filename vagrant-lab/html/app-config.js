window.FINANCE_APP_CONFIG = {
  // Para frontend + backend no mesmo domínio, deixe vazio.
  // Exemplo cross-domain: 'https://finance-api.seudominio.com'
  API_BASE_URL: 'https://devops-git-codex-create-expense-man-31c49d-jobarros89s-projects.vercel.app/',

  // Modo global (recomendado): exige backend online para todos os usuários
  // compartilharem os mesmos dados.
  REQUIRE_REMOTE: true
};
